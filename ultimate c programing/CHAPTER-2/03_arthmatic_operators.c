#include <stdio.h>

int main(){
    int a = 4;
    int b = 3;
    int c = a+b;
    printf("the value of a is %d and value of b is %d and sum is %d", a, b,c);
    // Modulas operatar id used to get remainder
    printf("the remainder when a is  devided by b is: %d\n", a%b);
// This is does not work in c 
//   int d= a^b
     return 0;
}
// int b = 2, c = 3;
//int z; z = b*c; //legal
//int z; b*c = z; //illegal (not allowed)*/
